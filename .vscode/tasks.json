{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang",
            "type": "shell",
            "command": "clang++ -Wall CPP_Primer_Notes.cpp -o check.out && ./check.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}